plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    // FFT (optional for later)
    implementation 'com.github.wendykierp:JTransforms:3.1'

    // SQLite (optional for later)
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

application {
    mainClass = 'soundwave.Main'
}
